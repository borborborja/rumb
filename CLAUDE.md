# Rumb — Claude Code

All agent guidance lives in the cross-tool file:

@AGENTS.md

Do not add rules here — edit `AGENTS.md` so every coding agent (Claude Code, opencode, …)
sees the same instructions.
